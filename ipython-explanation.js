// Generated by CoffeeScript 1.9.0
(function() {
  define([], function() {
    var loader;
    return loader = function(id, Explanation, d3, $) {
      var explanation, resize, section, wrapper, _window;
      explanation = Explanation(d3);
      wrapper = d3.select("#" + id).style({
        width: "100%",
        height: "1080px"
      }).call(explanation);
      if (window.Reveal) {
        section = d3.select($(wrapper.node()).parents("section").first()[0]);
        section.selectAll(".input, .prompt").remove();
        resize = function() {
          "wrapper.style\n  width: \"" + (window.innerWidth - 50) + "px\"\n  height: \"" + (window.innerHeight - 50) + "px\"\n\nsection.style\n  width: \"" + window.innerWidth + "px\"\n  height: \"" + window.innerHeight + "px\"\n  top: \"-" + (window.innerHeight / 2) + "px\"\n  left: \"-" + (window.innerWidth / 6) + "px\"";
          return explanation.resize();
        };
        _window = d3.select(window).on('resize.#{ id }', resize);
        return Reveal.addEventListener('slidechanged', function() {
          return resize();
        });
      }
    };
  });

}).call(this);
